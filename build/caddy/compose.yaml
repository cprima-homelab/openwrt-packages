services:
  build:
    build:
      context: .
    volumes:
      - ../../dist/caddy:/dist
      - caddy-gomodcache:/go/pkg/mod
      - caddy-gobuildcache:/root/.cache/go-build

volumes:
  caddy-gomodcache:
  caddy-gobuildcache:
