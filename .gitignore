.DS_Store
*.swp
*.tmp
.idea/
.vscode/

# OpenWrt SDK and build artifacts
openwrt-sdk*/
staging_dir/
build_dir/
bin/
dl/
*.ipk
*.apk
*.tar.gz
signing.key

# Stage 1 application build output (caddy binaries + SHA256SUMS)
dist/caddy/
!dist/caddy/.gitkeep

# Stage 2 OpenWrt package build output
dist/artifacts/

# Keep dist root anchor — dist/feed/ and dist/packages/ are generated, not committed
dist/*
!dist/.gitkeep
!dist/caddy/

# Local signing key output (private keys) — move to T:/netops/ immediately
keys/
